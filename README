Resurrect nml from >10 years of slumber.

Fixed runtime system to work for 64 bits.
Fix link order to ensure runtime system is initiialized first.

Get self compile working (using sml)
Get self compile working (using boot/nux)
